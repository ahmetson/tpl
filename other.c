/*
Save additional information.
*/
// Max len of token
	/*int token_MAXLEN = 0;
	while((c=fgetc(source))!=EOF) 
	{
		token_MAXLEN++;
	}
	fseek(source, 0, SEEK_SET);*/


/*// 1st STEP.
	// Pars and make algoritms
	while((c=fgetc(source))!=EOF) 
	{
		if (cmd_type=="no_cmd")
		{
			if (isspace(c)==0)
			{
				token[i] = c;
				i++;
			}
			else
			{
				token[i]='\0';
				if(strlen(token)>0)
				{
					printf("%s\n", token);
					empty_token(token);
				}
				
				i=0;
			}
			
			
		}
		//putchar(c);
	}
	token[i]='\0';*/
